# GENERATED by cc-guides from .claude/fragments/great-docs.yml/ — do not edit; edit the fragments and push — CI renders.
# Great Docs configuration — https://posit-dev.github.io/great-docs/
# Content-only site: no Python package; the landing renders from README.md.

display_name: cc-orchestrate
repo: https://github.com/yasyf/cc-orchestrate
site_url: "https://yasyf.github.io/cc-orchestrate/"
pypi: false

reference: false
mcp: false
skill:
  enabled: false
package_info_page: false

logo: docs/assets/logo.png

navbar_color: "#101f33"
accent_color:
  light: "#c1274a"
  dark: "#ff8ba0"

social_cards:
  image: docs/assets/social-preview.jpg
  twitter_site: "@yasyf"

hero:
  name: cc-orchestrate
  tagline: "Stop letting your Claude agents fight over one working copy."

# Composed by concatenation after each repo's local fragment: top-level keys
# here must not collide with any repo fragment (last key silently wins).
parser: google
github_style: widget
jupyter: python3
dark_mode_toggle: true
back_to_top: true
keyboard_nav: true
fast_sidebar: true
freeze: auto

# Fleet SEO: uniform page-title format (schema placeholders {page_title}/{site_name}; default
# separator is "|"). social_cards stays per-repo — repos set social_cards.image in their fragment.
seo:
  title_template: "{page_title} - {site_name}"
