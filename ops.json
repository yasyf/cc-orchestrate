{"v":1,"lamport":1,"ops":[{"kind":"create_note","nonce":"ab9da8015e0c93a5d477207deda8440e","title":"P1 method registry landed (op surface DRY primitive)","body":"Phase 1 of the XRPC plan (we-want-to-expose-ancient-raven) shipped. The daemon op surface is now generated from one table.\n\n**Where:** `orchestrate/registry.go` (table + adapter + error codes), `orchestrate/schema.go` (reflection → JSON-schema).\n\n**Contract for later phases:**\n- A `method{name,kind,desc,exposes,reqType,resType,adapt}` is built by generic `query[Req,Res](...)` / `procedure[Req,Res](...)`. The socket op string IS the method name (`cco.\u003cnoun\u003e.\u003cverb\u003e`, camelCase verbs). Add a method → append to the `methods` slice; `registerOps`, MCP tools, and (phase 2) XRPC pick it up automatically.\n- `expose` bits: `full`=socket|xrpc|mcp, `rpc`=socket|xrpc, `sock`=socket only. `cco.agent.report` is sock-only; the new show/list/unset getters are `rpc` (off parent MCP).\n- Error wire format: handlers return `(Res, error)`; `opErr(code, err)` tags it and the socket adapter serializes `\"\u003cCode\u003e: \u003cmessage\u003e\"` in `Reply.Error`. Codes: InvalidRequest|NotFound|Conflict|Unsupported|InternalError. Untagged → InternalError; store `errNotFound` sentinel → NotFound (via `replyError`). **Phase 2 XRPC bridge parses this prefix back to HTTP status.**\n- Strict decode: `decodeBody[Req]` uses DisallowUnknownFields; empty body → zero Req. `runTyped` is the shared adapter (tests call it too).\n- MCP tool name derivation: `mcpName` = strip `cco.`, `.`→`_`, camel→snake. Schemas are generated from req structs (per-field human descriptions dropped; method `desc` is the tool description).\n\n**Not yet added (later phases own them):** cco.fleet.status, cco.agent.capture, cco.agent.respawn, cco.sprint.kill. Table is trivially appendable.\n\n**Breaking:** all old op strings (\"agent-spawn\" etc.) are gone; agent-status → cco.agent.show (MCP agent_status → agent_show). Single-user, wire renamable.\u003c/body\u003e\n\u003cparameter name=\"paths\"\u003e[\"orchestrate/registry.go\", \"orchestrate/schema.go\"]","tags":["cc-orchestrate","xrpc-plan","phase-1"],"anchors":[]}]}