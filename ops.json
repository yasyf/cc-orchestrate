{"v":1,"lamport":1,"ops":[{"kind":"create_note","nonce":"ccc108ce8aeae40068b7c77b620bf6ad","title":"TUI framework evaluation for cco tui (2026-07): Bubble Tea v2 recommended","body":"Nine-agent research pass (mid-2026 state) over TUI frameworks for a `cco tui` front-end. Criteria in priority order: cc-* ecosystem fit, battle-tested, batteries included, fast. **Recommendation: Bubble Tea v2 + Charm stack (charm.land/bubbletea/v2, bubbles/v2, lipgloss/v2, glamour/v2). Not OpenTUI.** Pending user decision — this is the evaluation record, not a locked choice.\n\n## Why not OpenTUI (the \"obvious\" pick)\n- Cannot be embedded in a Go binary; its only Go bindings are explicitly unmaintained (as is @opentui/vue — only react/solid are dogfooded).\n- Core maintainer (simonklee, 2026-04-09, issue #316): \"still some time away from production ready\" for third-party use; roadmap explicitly scoped to \"what OpenCode and terminal.shop need\". Still 0.x (v0.4.3), weekly breaking-change cadence.\n- Distribution: a second Bun+Zig toolchain and a 50–136MB `bun build --compile` artifact outside GoReleaser — reverses the pure-Go single-static-binary decision.\n\n## Why Bubble Tea v2\n- **Integration (decisive)**: a Go TUI reuses `daemon.Client` (unix-socket RPC) + `consume.ConsumeEvents` (SSE resume/cursor/backoff) verbatim — `cco agent watch` (orchestrate/cli.go streamAgent/watchAllAgents) is already the exact pattern. A non-Go TUI must reverse-engineer an unversioned wire contract (no OpenAPI; op names + payload shapes are unexported Go structs), implement the unix-socket bootstrap (a pure-HTTP client is impossible — SSE port/subject_id only come from OpResolve/OpStatus), and hand-sync every payload struct. Raw transcript bytes never cross the wire — only derived orchestrate.status/inbound events — so a non-Go TUI can't even stream live panes without shelling out.\n- **Battle-tested**: v2 GA 2026-02-23 (\"Cursed Renderer\"/ultraviolet, synchronized output DEC 2026, Unicode mode 2027), dogfooded in Crush for months pre-GA. gh-dash, superfile, jjui, amux already on v2. The category converged on it: Claude Squad, Gas Town, amux — agent-fleet orchestrator TUIs — are all Go/Bubble Tea. opencode's migration off Bubble Tea was v1-era perf + the Charm ownership dispute; v2's renderer was built to close that gap.\n- **Batteries**: Bubbles v2 widgets, Glamour v2 (markdown transcripts), Huh v2 (spawn forms), lipgloss/table, teatest, `lrstanley/bubblezone/v2` for mouse hit-testing.\n- **Fast**: cell-diffing ncurses-style renderer; Crush streams LLM tokens through Program.Send in production.\n\n## Known gaps / gotchas\n- No tree widget (hand-roll the repo→workstream→sprint→agent tree) and no flexbox (Join/Place arithmetic; lipgloss#166 open for years; third-party: 76creates/stickers).\n- Pin `charmbracelet/ultraviolet`'s pseudo-version in lockstep with bubbletea/v2 — untagged pre-release; mismatched pair breaks compositors silently (amux's go.mod carries this exact warning).\n- teatest still in x/exp (no API guarantee); golden files flake on CI color-profile detection.\n- Async stdin race: input can be lost around ReleaseTerminal() — matters only if the TUI suspends to $EDITOR.\n- cc-orchestrate pins cc-interact v0.1.3 while sibling HEAD is v0.7.0+ (bearer auth, caught-up marker, Inject exist only at HEAD) — bump before TUI work.\n- Precedent (k9s/tview, lazygit/gocui, yazi/ratatui): serious dashboards often end up owning a framework fork; budget for it.\n- Crush uses `ncruces/go-sqlite3` (WASM, cgo-free) — the reference answer for cgo-free SQLite in a GoReleaser pipeline.\n\n## Runners-up\n- **Ratatui** (Rust): best-in-class framework health, but zero code reuse — serde mirrors of every Go struct, drift risk, second toolchain (GoReleaser has a Rust builder since v2.5, so pipeline friction is real but reduced).\n- **tview**: only stock option with TreeView/Flex/Grid, proven at k9s density — but k9s runs a frozen private fork, single maintainer (first tag ever Aug 2025), GitLab CLI is actively migrating off it to Bubble Tea.\n- Textual (Python), Ink (Node), and all emerging options (libvaxis, Notcurses, brick, ZigZag, tuie) disqualified on ecosystem fit / distribution; no dark-horse Go-viable candidate exists as of mid-2026.","tags":["tui","evaluation","architecture"],"anchors":[{"kind":"dir","value":"orchestrate"}]}]}